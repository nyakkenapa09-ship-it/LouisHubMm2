-- // Load Rayfield Library //
local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

-- // Create Window & Loader //
local Window = Rayfield:CreateWindow({
   Name = "Louis Hub MM2 V2",
   LoadingTitle = "Louis Hub Initializing...",
   LoadingSubtitle = "Injecting Script & Bypassing...",
   ConfigurationSaving = {
      Enabled = true,
      FolderName = "LouisHubFolder",
      FileName = "LouisHub_MM2"
   },
   Discord = {
      Enabled = true,
      Invite = "Q7TnV6dvC",
      RememberJoins = true
   },
   KeySystem = false
})

-- // Variables //
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local LocalPlayer = Players.LocalPlayer

local ESP_Enabled = false
local GunESP_Enabled = false
local SilentSheriff = false
local SilentMurder = false

-- // Tabs //
local VisualTab = Window:CreateTab("Visuals (ESP)", 4483362458)
local CombatTab = Window:CreateTab("Combat (Aim)", 4483362458)
local InfoTab = Window:CreateTab("Information", 4483362458)

-- // VISUALS TAB //
VisualTab:CreateSection("ESP Settings")

VisualTab:CreateToggle({
   Name = "Enable Player ESP (Outline)",
   CurrentValue = false,
   Flag = "PlayerESP",
   Callback = function(Value)
      ESP_Enabled = Value
   end,
})

VisualTab:CreateToggle({
   Name = "Enable Gun Drop ESP (Blue)",
   CurrentValue = false,
   Flag = "GunESP",
   Callback = function(Value)
      GunESP_Enabled = Value
   end,
})

-- // COMBAT TAB //
CombatTab:CreateSection("Silent Aim Features")

CombatTab:CreateToggle({
   Name = "Silent Aim Sheriff (Auto hit Murder)",
   CurrentValue = false,
   Flag = "AimSheriff",
   Callback = function(Value) SilentSheriff = Value end,
})

CombatTab:CreateToggle({
   Name = "Silent Aim Murderer (Auto hit others)",
   CurrentValue = false,
   Flag = "AimMurder",
   Callback = function(Value) SilentMurder = Value end,
})

-- // INFO TAB //
InfoTab:CreateSection("ESP Colors Guide")
InfoTab:CreateParagraph({Title = "🔴 Red", Content = "Murderer"})
InfoTab:CreateParagraph({Title = "🔵 Blue", Content = "Sheriff"})
InfoTab:CreateParagraph({Title = "🟡 Yellow", Content = "Hero (Innocent picking up Gun)"})
InfoTab:CreateParagraph({Title = "🟢 Green", Content = "Innocent"})
InfoTab:CreateParagraph({Title = "🔫 Blue", Content = "Gun Drop"})

-- ========================================== --
-- // LOGIC ESP (DARI KODE YANG KAMU KIRIM) //
-- ========================================== --

local function getRoleColor(player)
    local character = player.Character
    local backpack = player:FindFirstChild("Backpack")
    
    -- Check Murderer (Merah)
    if (backpack and backpack:FindFirstChild("Knife")) or (character and character:FindFirstChild("Knife")) then
        return Color3.fromRGB(255, 0, 0) -- Merah
    end
    
    -- Check Sheriff (Biru) / Hero (Kuning)
    if (backpack and backpack:FindFirstChild("Gun")) or (character and character:FindFirstChild("Gun")) then
        if player:FindFirstChild("RoleStat") and player.RoleStat.Value == "Sheriff" then
            return Color3.fromRGB(0, 0, 255) -- Biru
        else
            return Color3.fromRGB(255, 255, 0) -- Kuning
        end
    end
    
    -- Default Innocent (Hijau)
    return Color3.fromRGB(0, 255, 0) -- Hijau
end

-- Fungsi Apply ESP untuk Player
local function applyESP(player)
    if player == LocalPlayer then return end

    local conn
    conn = RunService.RenderStepped:Connect(function()
        if not player.Parent then
            conn:Disconnect()
            return
        end

        local character = player.Character
        if not character then return end

        local highlight = character:FindFirstChild("LouisHub_ESP")

        -- Jika toggle ESP Nyala
        if ESP_Enabled then
            if not highlight then
                highlight = Instance.new("Highlight")
                highlight.Name = "LouisHub_ESP"
                highlight.Parent = character
                highlight.FillOpacity = 0.5
                highlight.OutlineOpacity = 0
            end
            highlight.OutlineColor = getRoleColor(player)
            highlight.FillColor = getRoleColor(player)
        else
            -- Jika toggle ESP Mati, hapus highlight
            if highlight then
                highlight:Destroy()
            end
        end
    end)
end

-- Jalankan ESP Player
for _, p in pairs(Players:GetPlayers()) do applyESP(p) end
Players.PlayerAdded:Connect(applyESP)

-- Gun Drop Detection (Biru)
RunService.RenderStepped:Connect(function()
    for _, obj in pairs(workspace:GetChildren()) do
        if obj.Name == "GunDrop" or (obj:IsA("Model") and obj:FindFirstChild("Gun")) then
            local highlight = obj:FindFirstChild("LouisHub_GunESP")
            
            if GunESP_Enabled then
                if not highlight then
                    highlight = Instance.new("Highlight")
                    highlight.Name = "LouisHub_GunESP"
                    highlight.Parent = obj
                    highlight.FillColor = Color3.fromRGB(0, 0, 255) -- Biru
                    highlight.OutlineColor = Color3.fromRGB(0, 0, 255)
                    highlight.FillOpacity = 0.5
                    highlight.OutlineOpacity = 0
                end
            else
                if highlight then highlight:Destroy() end
            end
        end
    end
end)


-- ========================================== --
-- // LOGIC SILENT AIM (100% ACCURACY) //
-- ========================================== --

local oldNamecall
oldNamecall = hookmetamethod(game, "__namecall", function(self, ...)
    local method = getnamecallmethod()
    local args = {...}
    
    if not checkcaller() and method == "FireServer" then
        
        -- Silent Aim Sheriff -> Target Murderer
        if self.Name == "ShootGun" and SilentSheriff then
            for _, v in pairs(Players:GetPlayers()) do
                if v ~= LocalPlayer and v.Character and v.Character:FindFirstChild("HumanoidRootPart") then
                    local hum = v.Character:FindFirstChild("Humanoid")
                    -- Pengecekan Murderer
                    local isTargetMurder = (v.Character:FindFirstChild("Knife") or (v.Backpack and v.Backpack:FindFirstChild("Knife")))
                    
                    if hum and hum.Health > 0 and isTargetMurder then
                        for i, arg in pairs(args) do
                            if typeof(arg) == "Vector3" then
                                args[i] = v.Character.HumanoidRootPart.Position
                                return oldNamecall(self, unpack(args))
                            end
                        end
                    end
                end
            end
            
        -- Silent Aim Murderer -> Target Selain Murderer
        elseif self.Name == "ThrowKnife" and SilentMurder then
            for _, v in pairs(Players:GetPlayers()) do
                if v ~= LocalPlayer and v.Character and v.Character:FindFirstChild("HumanoidRootPart") then
                    local hum = v.Character:FindFirstChild("Humanoid")
                    -- Pengecekan Bukan Murderer
                    local isOtherMurder = (v.Character:FindFirstChild("Knife") or (v.Backpack and v.Backpack:FindFirstChild("Knife")))
                    
                    if hum and hum.Health > 0 and not isOtherMurder then 
                        for i, arg in pairs(args) do
                            if typeof(arg) == "Vector3" then
                                args[i] = v.Character.HumanoidRootPart.Position
                                return oldNamecall(self, unpack(args))
                            end
                        end
                    end
                end
            end
        end
        
    end
    return oldNamecall(self, ...)
end)

Rayfield:Notify({
   Title = "Louis Hub Loaded!",
   Content = "ESP & Silent Aim is working properly.",
   Duration = 5,
   Image = 4483362458,
})
